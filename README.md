# Shadowfax Geo Intelligence Portal

## IMPORTANT - Before making any changes
**Commit current working state to GitHub first:**
```
git init  # if not already done
git add .
git commit -m "stable: pre-enhancement checkpoint"
git remote add origin <your-repo-url>
git push -u origin main
```

## Deployment
Discuss deployment configuration on Monday. Target: Streamlit Cloud or internal server.

## Tech Stack
- Streamlit + Folium + BigQuery + Groq (Llama 3)
- See requirements.txt for full dependencies
