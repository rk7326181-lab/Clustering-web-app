# Copy to .env (repo root). Gitignored — never commit it. Loaded
# automatically on startup via python-dotenv (see top of app.py).
# BigQuery / login credentials live in .streamlit/secrets.toml instead —
# see .streamlit/secrets.toml.example.
GROQ_API_KEY=your_groq_api_key_here
